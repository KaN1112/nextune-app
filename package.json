{
  "name": "nextune",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:exe": "tauri build --no-bundle",
    "test": "node --test tests/*.test.js",
    "test:ui": "node tests/browser-navigation.mjs && node tests/browser-flows.mjs",
    "preview": "node scripts/preview.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.0",
    "playwright": "^1.63.0"
  }
}
