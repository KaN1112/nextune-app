# 検証記録

2026-09-14、Windows x64で実施。

## 日本語版の追加検証

7画面のメニュー、案内、確認・キャンセル、操作結果、スキャンカテゴリ、復元状態、エラー、日時を日本語化しました。NSISの表示言語はJapaneseに固定しています。

日本語版でもJavaScript 6件、Rust単体12件、ブラウザーの7画面・確認フローのテストが成功しました。読み取り専用実機テスト用exeの再実行はWindowsのアプリケーション制御で拒否されたため、日本語の製品版exeでPC情報、CPU/RAM、ディスク／ネットワーク監視、スキャン、20回のローカルPing、日本語のIP入力エラーを確認しました。ポリシーの変更は行っていません。

Windows API名、固定PowerShellコード、パフォーマンスカウンター名、IPCキー、設定JSONと履歴の内部値は翻訳せず維持しています。

最終結果：JavaScript 6件、Rust単体12件、Windows読み取り実機1件が成功。UIナビゲーションと確認フローの追加テストも成功。最終ソースからスタンドアロンexeとNSISセットアップexeを生成しました。セットアップのインストール／アンインストール自体は未検証です。

## 自動テスト

- JavaScript：リングバッファ、欠測、スコア重み、ゼロ測定値、パケット損失。
- Rust：Ping統計、設定ファイルの原子的置換、除外入力検証。
- クリーナー：隔離fixtureだけを削除、変更済みファイルを保持、異なるルートを拒否、使用中ファイルを保持、期限切れを拒否。
- 復元：変更前intentの保存と復元、外部設定変更の保持、パストラバーサル入力の拒否。
- Windows読み取り実機テスト：PC情報、CPU/RAM、ネットワーク構成、電源プラン、候補プロセス、クリーナースキャン、127.0.0.1に20回Ping。

## UI

- Microsoft Edgeで7画面を移動。1280×720 / 1440×900で横方向のオーバーフローなし。
- ブラウザプレビューでOS操作が拒否されることを確認。
- テスト専用IPC fixtureで、Boost / クリーナー / 復元のキャンセル、明示確認、選択された項目だけの送信、実行後の再スキャン、設定保存、ライトテーマ、Ping結果表示を確認。
- 生成exeのWebView2へ接続し、実IPCでPC情報、CPU/RAM、Boostスキャン、クリーナースキャン、20回のローカルPing、履歴、パフォーマンス、設定を確認。
- 納品用exeでもディスク転送量とネットワーク通信量の実カウンター取得を確認。
- Ping中に別ページへ移動して戻った場合の完了・再実行、および監視停止中のグラフ欠測処理を確認。
- 開発時のブラウザデバッグポートはテスト起動の環境変数だけに設定。配布設定にはありません。

## 実施していないこと

- 開発端末の電源プラン変更、ユーザーアプリの終了、実Temp削除は実施していません。
- Windows 10、別GPUベンダー、管理者ユーザー、破損したWindowsパフォーマンスカウンター、各種アンチウイルス環境の網羅検証は未実施です。
- ClippyはOSのアプリケーション制御ポリシーが`clippy-driver.exe`の起動を拒否したため実行できませんでした。ポリシー変更は行っていません。

公開配布前はREADMEの制限を確認し、隔離VMで実設定変更・復元・インストール／アンインストールの追加検証を推奨します。

## 公開用プレビュー追加検証（2026-09-14）

- JavaScript 6件、Rust 12件が成功（読み取り専用統合テスト1件は通常実行ではignore）。
- 8画面のナビゲーション、1280×720 / 1440×900の横幅を確認。
- IPC fixtureで、メモリ整理の1クリック送信と結果表示、処理中のページ移動後の復帰、アプリ検索、終了確認のキャンセル、PID＋起動時刻だけの正確な送信を確認。
- 生成Windowsアプリで、実アプリ一覧の取得と無効PID終了要求の拒否を確認。
- 本番用メモリ整理PowerShellを全対象除外で実行し、対象0件の結果を確認。EmptyWorkingSetを検証用PowerShellプロセス自身に適用して成功を確認。
- ユーザーアプリに対する実際の終了・メモリ整理、対応アプリごとの最小化挙動、別PCへのインストールは未検証。
- 未署名。公開サーバーへのアップロードは実施していません。
